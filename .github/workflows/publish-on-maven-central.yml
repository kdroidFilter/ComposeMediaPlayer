name: Publish to Maven Central

on:
  push:
    tags:
      - 'v*'

jobs:
  build-natives:
    uses: ./.github/workflows/build-natives.yml

  publish:
    needs: build-natives
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download macOS ARM64 library
        uses: actions/download-artifact@v4
        with:
          name: native-darwin-aarch64
          path: mediaplayer/src/jvmMain/resources/composemediaplayer/native/darwin-aarch64/

      - name: Download macOS x86_64 library
        uses: actions/download-artifact@v4
        with:
          name: native-darwin-x86-64
          path: mediaplayer/src/jvmMain/resources/composemediaplayer/native/darwin-x86-64/

      - name: Download Linux x86_64 library
        uses: actions/download-artifact@v4
        with:
          name: native-linux-x86-64
          path: mediaplayer/src/jvmMain/resources/composemediaplayer/native/linux-x86-64/

      - name: Download Linux aarch64 library
        uses: actions/download-artifact@v4
        with:
          name: native-linux-aarch64
          path: mediaplayer/src/jvmMain/resources/composemediaplayer/native/linux-aarch64/

      - name: Download Windows x64 library
        uses: actions/download-artifact@v4
        with:
          name: native-win32-x86-64
          path: mediaplayer/src/jvmMain/resources/composemediaplayer/native/win32-x86-64/

      - name: Download Windows ARM64 library
        uses: actions/download-artifact@v4
        with:
          name: native-win32-arm64
          path: mediaplayer/src/jvmMain/resources/composemediaplayer/native/win32-arm64/

      - name: Verify all natives present
        run: |
          EXPECTED=(
            "mediaplayer/src/jvmMain/resources/composemediaplayer/native/darwin-aarch64/libNativeVideoPlayer.dylib"
            "mediaplayer/src/jvmMain/resources/composemediaplayer/native/darwin-x86-64/libNativeVideoPlayer.dylib"
            "mediaplayer/src/jvmMain/resources/composemediaplayer/native/linux-x86-64/libNativeVideoPlayer.so"
            "mediaplayer/src/jvmMain/resources/composemediaplayer/native/linux-aarch64/libNativeVideoPlayer.so"
            "mediaplayer/src/jvmMain/resources/composemediaplayer/native/win32-x86-64/NativeVideoPlayer.dll"
            "mediaplayer/src/jvmMain/resources/composemediaplayer/native/win32-arm64/NativeVideoPlayer.dll"
          )
          MISSING=0
          for f in "${EXPECTED[@]}"; do
            if [ -f "$f" ]; then
              echo "OK: $f ($(wc -c < "$f") bytes)"
            else
              echo "MISSING: $f" >&2
              MISSING=1
            fi
          done
          if [ "$MISSING" = "1" ]; then exit 1; fi

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Publish to Maven Central
        run: ./gradlew publishAndReleaseToMavenCentral --no-configuration-cache
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVENCENTRALUSERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVENCENTRALPASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.SIGNINGINMEMORYKEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyId: ${{ secrets.SIGNINGKEYID }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNINGPASSWORD }}
